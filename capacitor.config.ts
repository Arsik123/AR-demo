import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId:    'com.example.mindar',  // любое ID
  appName:  'MindAR Demo',
  webDir:   'www',                 // папка c index.html
  bundledWebRuntime: false
};
export default config;
